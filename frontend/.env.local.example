# Perfil LOCAL — backend en tu PC, probado en celular físico por Wi-Fi.
# Copia a ".env" y reemplaza la IP por la de TU PC (ipconfig / ifconfig).
#   cp .env.local.example .env
EXPO_PUBLIC_API_URL=http://192.168.18.21:3000/v1
APP_ENV=local
