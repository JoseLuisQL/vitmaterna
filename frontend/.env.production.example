# Perfil PRODUCCIÓN — backend público por HTTPS.
# Copia a ".env" y reemplaza por tu dominio real.
#   cp .env.production.example .env
EXPO_PUBLIC_API_URL=https://api.vitmaterna.pe/v1
APP_ENV=production
