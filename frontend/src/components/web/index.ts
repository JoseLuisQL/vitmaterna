/**
 * VITMATERNA — Componentes del portal web (cáscara de escritorio).
 * Solo se activan cuando `useResponsive().webShell` es true.
 */
export { WebShell } from './WebShell';
export { WebSidebar } from './WebSidebar';
export { WebTopBar } from './WebTopBar';
